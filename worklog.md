# Worklog

---
Task ID: 1
Agent: Main Agent (Super Z)
Task: بناء تطبيق ويب تفاعلي لاختبار تحديد مستوى اللغة الإنجليزية للطلبة (ويبينار الدكتورة دعاء) مع لوحة أدمن

Work Log:
- بحث في GitHub عن مصادر تفاعلية: topics/kids-learning (CEFR kids apps), lmammino/awesome-learn-by-playing, topics/quiz-app — واعتماد أنماط: مستويات CEFR، نجوم وشارات، أسئلة مصورة، تغذية راجعة فورية
- تهيئة بيئة fullstack (Next.js 16 + Prisma + shadcn/ui)
- توليد صورتين بالذكاء الاصطناعي: بومة معلمة (mascot-welcome.png) وشخصيات احتفال (mascot-celebrate.png) في public/images
- قاعدة البيانات: نموذج Student (الاسم، الهاتف، العمر، الدولة، الدرجة، النسبة، المستوى، طلب المقابلة، الإجابات) — prisma db push تم
- ملفات البيانات: src/lib/quiz-data.ts (20 سؤالاً × 5 مستويات A1-C1: صور، قواعد، مفردات، قراءة)، src/lib/config.ts (كلمة مرور الأدمن duaa2026 + رقم الفريق الأكاديمي)، src/lib/sounds.ts (أصوات Web Audio بدون ملفات)
- API: src/app/api/students/route.ts — POST تسجيل، PATCH نتائج/مقابلة، GET قائمة (محمي بمفتاح)، DELETE حذف
- الواجهات: WelcomeScreen، RegisterForm (تحقق كامل)، QuizScreen (تغذية راجعة فورية + كونفيتي + أصوات + شريط تقدم + نجوم + بومة مشجعة)، ResultScreen (شارة المستوى + نجوم + وصف + توصية دورة + حجز Zoom + أزرار اتصال/واتساب 042899688)، AdminDashboard (بوابة كلمة مرور، 4 بطاقات إحصائية، توزيع المستويات، بحث وفلترة، جدول طلبة مع اتصال/واتساب/حذف، تصدير CSV)
- RTL كامل بخط Baloo Bhaijaan 2 العربي الدائري المناسب للأطفال
- اختبار شامل بالمتصفح (agent-browser): تدفق كامل من التسجيل حتى النتيجة، حجز مقابلة، دخول الأدمن، تجاوب الجوال — لا أخطاء console
- تنظيف بيانات الاختبار من قاعدة البيانات ليبدأ الويبينار ببيانات حقيقية

Stage Summary:
- تطبيق كامل يعمل على / (صفحة واحدة متعددة المشاهد + API routes)
- كلمة مرور الأدمن: duaa2026 (قابلة للتغيير من src/lib/config.ts)
- المستويات: أقل من 35% A1، 35-54% A2، 55-69% B1، 70-84% B2، 85%+ C1
- زر الإدارة: زر عائم أسفل يسار الشاشة
